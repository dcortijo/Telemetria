#pragma once

enum EventType {
	SessionStart,
	SessionEnd,
	RoundStart,
	RoundEnd,
	Attack,
	Ability,
	ReceiveDamage,
	ERROR
};

enum Players {
	PLAYER1, PLAYER2
};

enum Characters {
	MKWH00P, F10R, Mockingbird
};

enum Attacks {
};

enum Abilities {
};